services:
  nginx:
    image: nginx:latest
    # 可以在这里添加 Nginx 的额外配置
  mysql:
    image: mysql:latest
    # 配置 MySQL 的参数，如环境变量、挂载卷等
  redis:
    image: redis:latest
    # 配置 Redis 的参数
  rabbitmq:
    image: rabbitmq:latest
    # 配置 RabbitMQ 的参数
  elasticsearch:
    image: elasticsearch:latest
    # 配置 Elasticsearch 的参数
  maxwell:
    image: zendesk/maxwell:latest
    # 配置 Maxwell 的参数
